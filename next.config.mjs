/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "fakestoreapi.com",
            "encrypted-tbn0.gstatic.com",
        ]
    }
};

export default nextConfig;
