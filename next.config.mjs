/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "faketoreapi.com",
            "ead.ifms.edu.br"
            
        ]
    }
};

export default nextConfig;
